{
  "version": 2,
  "buildCommand": "echo No project build needed - api files are compiled independently by the Vercel Node runtime",
  "installCommand": "npm install",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
